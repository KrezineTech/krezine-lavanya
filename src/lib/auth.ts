// Authentication removed - this is a stub file
// All authentication logic has been removed from the application

export const auth = null;
export const signIn = null;
export const signOut = null;
export const handlers = { GET: null, POST: null };

// Auth options stub
const authOptions = {};

export default authOptions;